[
  {
    "name": "Browse Library shows empty state",
    "steps": [
      {
        "send": { "callback": "library:curated" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "The library is empty. Check back soon — new tracks are added regularly."
            }
          }
        ]
      }
    ]
  }
]
