[
  {
    "name": "/upload requires admin access",
    "steps": [
      {
        "send": { "text": "/upload" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Admin access required. Contact the bot owner for permissions."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "upload button requires admin access",
    "steps": [
      {
        "send": { "callback": "menu:upload" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Admin access required."
            }
          }
        ]
      }
    ]
  }
]
