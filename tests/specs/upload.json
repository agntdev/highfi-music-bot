[
  {
    "name": "/upload requires admin access",
    "steps": [
      {
        "send": { "text": "/upload" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Akses admin diperlukan. Hubungi pemilik bot untuk izin."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "upload button requires admin access",
    "steps": [
      {
        "send": { "callback": "menu:upload" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Akses admin diperlukan."
            }
          }
        ]
      }
    ]
  }
]
